import { FiltroUniPipe } from './filtro-uni.pipe';

describe('FiltroUniPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUniPipe();
    expect(pipe).toBeTruthy();
  });
});
